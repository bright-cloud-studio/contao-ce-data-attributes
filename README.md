# contao-ce-data-attributes

Adds a Data Attributes section to the backend. Data Attributes can then be selected on a content element and a value can be filled in. That Data Attribute then renders on the Content Element in the frontend.
